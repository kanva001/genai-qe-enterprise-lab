# Sprint 0 Plan (Foundation)

Goals:
- Establish QE toolchain and repo structure
- Create baseline smoke suites (UI + API)
- Create DB evidence schema and first reconciliation queries
- Create initial CI workflow skeleton

Backlog (Sprint 0):
1) Add Test Strategy + Risk Based Testing doc
2) Setup Java Selenium TestNG Maven project + smoke tests
3) Add Postman collection + Newman runner
4) Add SQL Server evidence DB schema + reconciliation queries
5) Add JMeter folder + performance plan template
6) Add CI workflow for smoke suites